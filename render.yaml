services:
  # Web Service (Flask App)
  - type: web
    name: travel-planner
    env: python
    region: singapore  # Or choose: oregon, frankfurt, ohio, singapore
    plan: free  # Free tier
    buildCommand: "./build.sh"
    startCommand: "gunicorn wsgi:app"
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true  # Render will auto-generate a secure key
      - key: DATABASE_URL
        fromDatabase:
          name: travel-db
          property: connectionString
      - key: OPENROUTER_API_KEY
        sync: false  # You'll add this manually in Render dashboard
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SESSION_COOKIE_SECURE
        value: true
    healthCheckPath: /

databases:
  - name: travel-db
    databaseName: travel_db
    user: travel_user
    plan: free  # Free PostgreSQL (90 days, then you need to upgrade or use external DB)
